# 发布到 Gitee（尽量不折腾）

你现在已经有一个本地的「资源中心」文件夹了。要让大家都能用，需要把它发布成一个 Gitee 仓库。

下面给两种方式：**小白优先用方式 A**。

## 方式 A（推荐）：网页创建仓库 + 双击脚本发布

1. 打开 Gitee 网页，创建一个新仓库  
   仓库名建议：`y3-resource-hub`（你也可以改成你喜欢的名字）
2. 创建完成后，复制仓库的 HTTPS 地址  
   形如：`https://gitee.com/用户名/y3-resource-hub.git`
3. 回到本地文件夹，双击运行：`一键发布到Gitee.bat`
4. 按脚本提示粘贴仓库地址回车
5. 如果 Gitee 提示登录：用账号 + 私人令牌（PAT）登录即可（Gitee 后台可生成 PAT）

完成后，你的仓库里就会生效 Issue 表单模板（`.gitee/ISSUE_TEMPLATE`），作者就能开始填表投稿。

## 方式 B：你会 Git（可选）

如果你会 Git，也可以在命令行里执行类似步骤：
- `git init`
- `git add .`
- `git commit -m "init resource hub"`
- `git branch -M main`
- `git remote add origin <你的仓库地址>`
- `git push -u origin main`

## 发布后要改 1 个地方（很重要）

`.gitee/ISSUE_TEMPLATE/config.yml` 里有两个 `url:` 现在是占位链接。  
发布后，把它们改成你仓库里的：
- `INDEX.md` 页面链接
- `CONTRIBUTING.md` 页面链接

